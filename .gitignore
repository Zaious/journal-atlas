# Cache
.cache/
__pycache__/
*.pyc

# Dependencies (demo/backend + demo/frontend)
.venv/
node_modules/

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/

# Env
.env
*.env

# Workspace (external to repo)
# These directories live in the parent workspace (P:\MyOpenSource\JournalMatchEvaluator\)
# and contain internal strategic analysis, competitive research, and source material
# that must never be committed to this public repo.
.ark/
ref/
ref-research/
skill-forge/
AGENTS.md
dist/

# Eval run artifacts (regenerable; the eval definitions in
# skills/journal-atlas/evals/ ARE tracked)
skills/journal-atlas-workspace/
.claude/

# wrangler local cache — account id and project bookkeeping, not source
.wrangler/

# Written by demo/backend/build_version.py at deploy time (PREDEPLOY).
# Not committed: a file cannot record the hash of the commit containing it, so
# every committed copy would name its own parent. What a human writes lives in
# demo/backend/paper_version.json, which is committed.
demo/backend/version.json

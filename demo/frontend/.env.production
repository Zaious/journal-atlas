# Build-time config for the public deployment. Committed deliberately: these
# are public URLs, not secrets, and a reproducible build beats a value that
# only exists in whoever's shell ran the deploy.
VITE_API_BASE=https://api.journal-atlas.chroniclecore.com
